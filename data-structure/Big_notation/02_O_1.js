// * imagine you have a box fill with items , and yoou know where each item is located. to get a specifiq item you go directly to its location. tking the same amount of time irrespective of how many items are in the box

// ! O(1)

const numbers = [1,2,3,4,5,6]

const getElement = (arr,index) => arr[index]

console.log(getElement(numbers,2))

